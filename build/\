cmake_minimum_required(VERSION 2.8)
project( display )
find_package( OpenCV REQUIRED )
find_package( raspicam REQUIRED )
add_executable( display stuff.cpp )
target_link_libraries( display ${OpenCV_LIBS} ${raspicam_CV_LIBS} )
